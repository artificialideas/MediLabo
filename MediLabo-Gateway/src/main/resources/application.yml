server:
  port: 9000

spring:
  application:
    name: "MediLabo-Gateway"
  cloud:
    gateway:
      globalcors:
        '[/**]':
          allowedOrigins: "*"
          allowedMethods: "*"
          allowedHeaders: "*"
      routes:
        - id: patients_route
          uri: http://localhost:9001
          predicates:
            - Path=/patients-service/**
          filters:
            - SetPath=/patients/
        - id: angular_route
          uri: http://localhost:4200/
          predicates:
            - Path=/**